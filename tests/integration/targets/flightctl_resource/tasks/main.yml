---
- include_tasks: update-device.yml
- include_tasks: fleet-lifecycle.yml
- include_tasks: repository-lifecycle.yml
- include_tasks: resourcesync-lifecycle.yml
- include_tasks: idempotency.yml
# Disabled: FlightCtl service returns 500 for TemplateVersion delete operations.
# Re-enable when the backend is fixed: uncomment the line below.
# - include_tasks: delete-template-versions.yml
